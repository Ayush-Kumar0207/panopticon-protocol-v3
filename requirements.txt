# Core framework
fastapi==0.135.3
uvicorn[standard]==0.44.0
pydantic==2.12.5

# HTTP client
httpx==0.28.1

# LLM client (for inference.py)
# OpenEnv-facing dependencies are explicit so upstream releases cannot silently
# change the canonical environment or make the lock unresolvable.
openai==2.30.0

# OpenEnv core (required for hackathon validation)
openenv-core==0.2.3
fastmcp==3.2.4
gradio==6.11.0

# RL training stack (Native PyTorch)
torch==2.2.1
gymnasium==0.29.1
stable-baselines3==2.2.1
numpy==1.26.4
tqdm==4.66.5

# LLM fine-tuning and local evaluation stack
# Keep these aligned with the HF Space container to avoid transformers /
# huggingface-hub compatibility issues during local inference.
trl==0.12.2
transformers==4.46.3
tokenizers==0.20.3
accelerate==0.34.2
peft==0.12.0
datasets==3.1.0
huggingface-hub==0.36.2
safetensors==0.4.5

# Visualization
matplotlib==3.9.4
