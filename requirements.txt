# Core framework
fastapi==0.110.0
uvicorn[standard]==0.27.1
pydantic==2.6.1

# HTTP client
httpx==0.26.0

# LLM client (for inference.py)
# openenv-core requires OpenAI 2.7.2+.
openai>=2.7.2,<3.0.0

# OpenEnv core (required for hackathon validation)
openenv-core==0.2.1

# RL training stack (Native PyTorch)
torch==2.2.1
gymnasium==0.29.1
stable-baselines3==2.2.1
numpy==1.26.4
tqdm==4.66.1

# LLM fine-tuning and local evaluation stack
# Keep these aligned with the HF Space container to avoid transformers /
# huggingface-hub compatibility issues during local inference.
trl==0.12.2
transformers==4.46.3
tokenizers==0.20.3
accelerate==0.34.2
peft==0.12.0
datasets==3.1.0
huggingface-hub==0.26.5
safetensors==0.4.5

# Visualization
matplotlib>=3.8.0
