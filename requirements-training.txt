# Canonical training/evaluation environment. This is intentionally isolated
# from the OpenEnv/FastMCP/Gradio server stack so unrelated server dependency
# releases cannot change or block a training run.
# GPU contributors must use tools/install_canonical_training_env.py. It selects
# the official cu121 index for torch; a direct generic pip install can select a
# CPU wheel and is therefore not a canonical installation procedure.
fastapi==0.135.3
httpx==0.28.1
torch==2.2.1
gymnasium==0.29.1
stable-baselines3==2.2.1
numpy==1.26.4
pydantic==2.12.5
trl==0.12.2
transformers==4.46.3
tokenizers==0.20.3
accelerate==0.34.2
peft==0.12.0
datasets==3.1.0
huggingface-hub==0.26.5
safetensors==0.4.5
matplotlib==3.9.4
tqdm==4.66.5
pytest==8.3.5
